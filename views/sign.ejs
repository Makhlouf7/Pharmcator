<!DOCTYPE html>
<html lang="en" data-page="login">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PHARMCATOR | Login</title>

    <!-- Style -->
    <link rel="icon shortcut" href="../images/favicon.png" type="image/png" />
    <link rel="stylesheet" href="../dist/index.css" />
    <link rel="stylesheet" href="../css/shared.css" />
    <link rel="stylesheet" href="../css/register.css" />
  </head>
  <body>
    <%- include("_header.ejs") %>
    <div class="container-fluid">
      <div class="row gap-sm-0 gap-5">
        <div class="form-image-container col-12 p-0">
          <div class="form-image">
            <form action="" class="col-sm-6 col-12 px-5 my-auto">
              <label for="email" class="form-label">Enter Your Email</label>
              <input
                type="text"
                id="email"
                name="email"
                class="form-control"
                placeholder="email@gmail.com"
                required
              />
              <small id="emailHelp" class="form-text text-muted mb-3 d-block"
                >You don't have an account ?
                <a class="fw-bold register-link" href="/register"
                  >Register</a
                ></small
              >
              <label for="inputPassword" class="form-label"
                >Enter Your Password</label
              >
              <input
                type="password"
                id="inputPassword"
                name="password"
                class="form-control"
                placeholder="password"
                required
              />
              <small id="emailHelp" class="form-text text-muted mb-3 d-block"
                >Can't remember your password ?
                <a class="fw-bold register-link" href="./forgotPassword "
                  >Forgot Password</a
                ></small
              >
              <small id="invalidMessage" class="form-text d-none text-danger">
                Incorrect email or password
              </small>
              <button
                type="submit"
                class="btn btn-success btn-bg-color mt-3 w-100"
              >
                Sign in
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <script src="../dist/index.js"></script>
  </body>
</html>
